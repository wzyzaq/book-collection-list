/**
 * 获取元素相对于另一个元素的位置（或者说相对于 body）
 * 感谢 `meouw`: http://stackoverflow.com/a/442474/375966
 */
export default function getRelativePosition(elm: HTMLElement, relativeElm?: HTMLElement): {
    top: number;
    left: number;
};
