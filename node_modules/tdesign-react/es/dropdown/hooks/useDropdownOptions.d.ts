import React from 'react';
import { DropdownOption } from '../type';
export declare const getOptionsFromChildren: (children: React.ReactElement<any>) => DropdownOption[];
export default function useDropdownOptions(children: (React.ReactElement | React.ReactNode | React.ReactPortal)[], options: DropdownOption[]): DropdownOption[];
