/**
 * 自定义显示列控制器，即列配置
 */
import React from 'react';
import { CheckboxGroupValue } from '../../checkbox';
import { PrimaryTableCol, TdPrimaryTableProps } from '../type';
export declare function getColumnKeys(columns: PrimaryTableCol[], keys?: Set<string>): Set<string>;
export default function useColumnController(props: TdPrimaryTableProps, extra?: {
    onColumnReduce?: (reduceKeys: CheckboxGroupValue) => void;
}): {
    tDisplayColumns: CheckboxGroupValue;
    renderColumnController: () => React.JSX.Element;
};
