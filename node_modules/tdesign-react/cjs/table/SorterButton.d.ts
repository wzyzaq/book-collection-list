import React, { MouseEvent } from 'react';
import { SortType } from './type';
import { TooltipProps } from '../tooltip';
import { TNode } from '../common';
export interface SorterButtonProps {
    sortType: SortType;
    sortOrder: string;
    sortIcon: TNode;
    tooltipProps?: TooltipProps;
    hideSortTips?: boolean;
    onSortIconClick: (e: MouseEvent<HTMLSpanElement>, p: {
        descending: boolean;
    }) => void;
}
declare function SorterButton(props: SorterButtonProps): React.JSX.Element;
declare namespace SorterButton {
    var displayName: string;
}
export default SorterButton;
