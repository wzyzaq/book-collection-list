import type { TdImageViewerProps } from '../type';
declare const useList: (images: TdImageViewerProps["images"]) => {
    mainImage: string | File;
    thumbnail: string | File;
    download: boolean;
    isSvg?: boolean;
}[];
export default useList;
