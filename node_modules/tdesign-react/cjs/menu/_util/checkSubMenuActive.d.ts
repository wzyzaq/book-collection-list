import React, { ReactElement } from 'react';
import { MenuValue } from '../type';
declare const checkSubMenuActive: (children: React.ReactNode, active: MenuValue) => ReactElement;
export default checkSubMenuActive;
