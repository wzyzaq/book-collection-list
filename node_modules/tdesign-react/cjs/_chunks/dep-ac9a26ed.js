/**
 * tdesign v1.12.0
 * (c) 2025 tdesign
 * @license MIT
 */

'use strict';

var _typeof = require('./dep-dab07be2.js');
var _baseGetTag = require('./dep-5dc79997.js');
var isObjectLike = require('./dep-350e0ec1.js');

/** `Object#toString` result references. */
var symbolTag = '[object Symbol]';

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return _typeof._typeof(value) == 'symbol' || isObjectLike.isObjectLike(value) && _baseGetTag.baseGetTag(value) == symbolTag;
}

exports.isSymbol = isSymbol;
//# sourceMappingURL=dep-ac9a26ed.js.map
