import React from 'react';
interface SelectArrowProps {
    isActive: boolean;
    isHighlight: boolean;
    disabled: boolean;
}
export declare const SelectArrow: ({ isActive, isHighlight, disabled }: SelectArrowProps) => React.JSX.Element;
export {};
