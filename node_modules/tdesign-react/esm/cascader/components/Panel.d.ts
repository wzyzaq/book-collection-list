import React from 'react';
import { CascaderContextType } from '../interface';
import { TdCascaderProps } from '../type';
import { StyledProps } from '../../common';
export interface CascaderPanelProps extends StyledProps, Pick<TdCascaderProps, 'trigger' | 'empty' | 'onChange' | 'loading' | 'loadingText'> {
    cascaderContext: CascaderContextType;
}
declare const Panel: (props: CascaderPanelProps) => React.JSX.Element;
export default Panel;
